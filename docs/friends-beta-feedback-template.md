# Friends Beta Feedback Template

Use this template for every issue reported during friends-only beta.

## Reporter
- Name:
- Browser + version:
- OS:
- Build version/tag:

## What happened
- Summary:
- Reproduction steps:
1. 
2. 
3. 

## Impact
- Data loss involved? (`yes` / `no`)
- Can user continue working? (`yes` / `no`)
- Workaround available? (`yes` / `no`)
- Workaround details:

## Severity
- Priority: `P0` (data safety / app unusable), `P1` (major stability), `P2` (UX)
- Suggested rollback trigger? (`yes` / `no`)

## Evidence
- Screenshot/video:
- Console error (if any):
- Local recovery used (`retry` / `import_backup` / `reset_workspace` / `none`):
