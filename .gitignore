node_modules/
build/*
!build/icon.icns
dist/
release/
coverage/
.DS_Store
*.log
.env*
.alma-snapshots
.worktrees/
test-results/
.noa/
CLAUDE.md
AGENTS.md

# Agent tooling — local to this machine, not shared via version control.
# Deliberately not ignoring all of .claude/ so a shared settings.json can be
# committed later; only the local overrides and installed skills are excluded.
.agents/
.claude/skills/
.claude/settings.local.json
skills-lock.json
