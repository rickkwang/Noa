node_modules/
build/*
!build/icon.icns
dist/
release/
coverage/
.DS_Store
*.log
.env*
.alma-snapshots
.worktrees/
test-results/.last-run.json
.noa/
CLAUDE.md
AGENTS.md

# Agent tooling — reinstallable from skills-lock.json, which IS tracked.
# Deliberately not ignoring all of .claude/ so a shared settings.json can be
# committed later; only the local overrides and installed skills are excluded.
.agents/
.claude/skills/
.claude/settings.local.json
