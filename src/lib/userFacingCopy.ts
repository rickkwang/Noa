export const LOCAL_DATA_BOUNDARY_COPY =
  'Your notes are stored in this browser/device profile only. Browser and desktop app data are separate unless you export/import manually. No automatic cloud sync.';

export const LOCAL_DATA_RECOMMENDED_FLOW_COPY =
  'Recommended workflow: Write locally -> export JSON backups regularly -> use import or the vault folder for cross-device use.';
